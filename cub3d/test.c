#include "cub3d.h"

int main(void)
{
    // void    *mlx_ptr;

    // mlx_ptr = mlx_init();

    // mlx_new_window(mlx_ptr, 500, 500, "sashin's window");

    // mlx_loop(mlx_ptr);
    printf("hi");
}